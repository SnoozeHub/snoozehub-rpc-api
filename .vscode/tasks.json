{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "gen",
            "type": "shell",
            "command": "protoc -I=. *.proto --js_out=import_style=commonjs,binary:frontend-gen/grpc_gen --grpc-web_out=import_style=typescript,mode=grpcweb:frontend-gen/grpc_gen && protoc -I=. *.proto --go_out=backend-gen/grpc_gen --go_opt=paths=source_relative --go-grpc_out=backend-gen/grpc_gen --go-grpc_opt=paths=source_relative"
        }
    ]
}